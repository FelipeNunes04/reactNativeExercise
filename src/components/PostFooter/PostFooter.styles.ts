import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  footer: {
    marginTop: 10,
  },
  footerName: {
    fontSize: 16,
    fontWeight: 'bold',
  },
  description: {
    fontSize: 14,
  },
  createdAt: {
    fontSize: 12,
    color: 'gray',
  },
});

export default styles;
